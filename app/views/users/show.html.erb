<% provide(:title, @user.name) %>

<p class="content">
	<p class="text">
		You account information:</br>
		Name: <%= @user.name %></br>
		Email: <%= @user.email %>
		<% if @user.posts.any? %>
      	<h3>Posts (<%= @user.posts.count %>)</h3>

<%-# Printing out all the posts of chosen user -%>

 		 <% @posts.each do |i| %>
 		 	<li>
 		 	This post was created at: <%= i.created_at %></br>
 		 	Contents of the post: <%= i.content %>
			<%= link_to "Delete this post", @post, method: :delete, class: "black", data: { confirm: "You sure?" } %>
 		 	</li>
 		 <% end %>
    	<% end %>
	</p>
	