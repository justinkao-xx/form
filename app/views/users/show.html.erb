<% provide(:title, @user.name) %>

<p class="content">
	<p class="text">
		You account information:</br>
		Name: <%= @user.name %></br>
		Email: <%= @user.email %>
		<% if @user.posts.any? %>
      	<h3>Posts (<%= @user.posts.count %>)</h3>
        <%= @posts %>
    	<% end %>
	</p>
	