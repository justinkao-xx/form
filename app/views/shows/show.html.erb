<% provide(:title, @show.name.capitalize ) %>

<h2 align="center">About: <%= @show.name.capitalize %></h2>
<table align="center">
	<tr>
		<td align="left"><b>Name</b></td>
		<td align="left"><b>Description</b></td>
		<td align="left"><b>Year created</b></td>
		<td align="left"><b>Seasons</b></td>
	</tr>
 	<tr>
 		<td align="left"><%= @show.name.capitalize %></td>
 		<td align="left"><%= @show.description.capitalize %></td>
 		<td align="left"><%= @show.created_at %></td>
 		<td align="left"><%= @show.seasons %></td>
 	</tr>
</table>
<%= link_to "Edit show", edit_show_path, class: "black" %> or
<%= link_to "Back to previous page", shows_path, class: "black" %>
	
	