<% provide(:title, "New movie") %>

<p class="content">
    Create a new movie into our database:
    <%= form_for(@movie) do |f| %>
    	<div class="forms">
    		<%= f.text_field :name, placeholder: "Name of the movie...", autofocus: true %>
			<%= f.text_area :description, placeholder: "Describe the movie..." %>
			<%= f.text_field :year, placeholder: "When was it released..." %>
			<%= f.submit 'Submit!' %>
		</div>
	<% end %>
</p>