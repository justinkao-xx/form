<% provide(:title, "Converter") %>
<p class="content">
	<p class="text">
		You can convert EUR to any currency in the drop-down menu!</br>
		Currency rates are updated every bank-day, from the web.
		
	</p>
<%= form_tag(form_converter_path) do %>
	<div class="forms">
	<%= label_tag("Amount:") %>
	<%= text_field_tag(:number, nil, placeholder: "Enter a number here...")%>
	
	<%= label_tag("Exchange rate:") %>
	<%= text_field_tag("Exchange rate", number_to_currency(@arv, separator: ",", precision: 4), :readonly => true)%>
	<%= select_tag(:rate, options_for_select(@currency_list)) %>
	<%= submit_tag 'Calculate!' %>
	</div>
<% end %>
</p>
